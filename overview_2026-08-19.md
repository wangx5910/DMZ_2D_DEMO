# 海湾城 2D Demo · 本轮交付概览（2026-08-19）

## 一、修掉的阻断性 bug（world_map.gd 编译失败）
用临时 `tools/_diag_preload.gd` 单独 `load` 被预载链吞掉内部错误的脚本，逐个定位并修复：
1. **缩进 bug**：`_tick_match` 里落地判定三行没缩进进 `if` 块 → "Expected indented block"。
2. **飞船巡航逻辑错**：原按"飞到两个路点"距离判断（≈320 秒），完全没用 `spaceship_cruise_time` 时长参数 → 飞船永远到不了降落。改为**按巡航时长触发**。
3. **`find_clear_circle` 全图随机取点**：降落点离飞船极远、下降超时。加 `near` 偏置（飞船当前位置 ±1600px，边缘余量 2000px），降落点真正贴近飞船。
4. **撤离逻辑 else 挂错 if（根因）**：每帧 `extract_t += delta` 后又因"未达阈值"被 `extract_t -= delta*1.5` 净减，永远到不了阈值 → 撤离永远失败。改为把"达阈值则撤离"嵌进 `in_zone` 分支。

## 二、确认并收尾的大锤明确要求
- **怪物视野范围淡显：移除**（"把怪物的视野范围给清掉吧"）。
- **玩家圆形视野：已实现**（扇形外以自身为圆心点亮一圈 360° 近身视野，`proximity_radius` 0→160，满亮度）。scene_test 断言相应改为"正后方 100px（圆内）可见 / 260px（圆外）不可见"。
- **玩法流程：跑通**（"出生点→公共撤离点→5分钟刷飞船巡航→降落成富物资 POI"）。`--flow-test` 全绿。

## 三、测试重标定（chase-test）
- 旧版无种子 + 把"绕墙绕路净进展低"误算"严重卡住" → 60~75% 浮动噪声。
- 重标定：区分**真冻结**（没到且几乎没动）与**绕路**（没到但走了 600~2400px），加 `seed` 固定场景。
- **结论：真冻结 0%（跨运行一致）**，与 `stuck_test`（巡逻 0% 卡墙）互相印证——AI 彻底不冻结。

## 四、测试结果（8 套全绿）
flow / scene / stuck / chase / world / ai / vehicle / loot 全部通过。删除了临时诊断文件。

## 五、文档同步
- `2D-Demo设计规范.md` v0.2→v0.3：O2 圆周视野【已定】、新增 §七 玩法流程【已定】、数值总表补玩法流程参数、O8 待议。
- `跑测自查清单.md` v0.1→v0.2：新增 §六 玩法流程手动验收项。
- `协作指令.md`：变更记录补阶段二实现启动。

## 六、待大锤拍板
- O1 视野外全黑 vs 压暗（现值 0.45）；O8 飞船 POI 富度档/守卫数/刷出时机；L5~L12 系列。
