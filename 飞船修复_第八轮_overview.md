# 飞船可用性修复 · 第八轮（2026-08-19）

## 你反馈的三个问题 → 已全部修复并实测通过

1. **上不去**：之前飞船落地会给它套一圈围墙 POI，光柱入口在船体内部被墙挡死，玩家走不进去。
   现改为：落地只在船内撒富物资容器（无墙可走入），并用 `_pick_land_spot` 保证**四个光柱口都不压在 POI 墙上**。
2. **劫不了**：之前劫持判定被钉死在"仅巡航期"，而你按 F2 召唤是"已降落(LANDED)"状态，登舰后永远不满足劫持条件。
   现改为：劫持判定放开到 **巡航 / 下降 / 已降落** 三种状态。
3. **走不完流程**：全链路实测 **登舰 → 操控室劫持 → 飞向破解点 → 驻守 4 分钟 → 整船撤离 + 密闭舱奖励（无视负重）** 全部跑通。

## 本轮额外抓到的坑（重要）

- 上一轮我报的"14/14 无 flake"是**假绿**：提交进仓库的测试脚本 A4 段没走游戏真实落点逻辑（直接用了粗略的 `find_clear_circle`），所以 A6（光柱口不压墙）时灵时不灵。本轮首跑就抓到 A6 失败。
- 加固时我一度把落点改成"全图兜底搜"，结果偶发把飞船甩到地图另一端，自然降落下降距离过长、超时，flow 测试翻车。已改回 **"就近 60 次重试找四口全清，实在找不到就退回最近的近处空地"**——既确定、又绝不跨图远降。

## 最终验证

- 编译：无解析错误。
- 飞船全链路（`--ship-test`）：**连跑 5 遍全绿（14/14）**，A6 现在确定不 flake。
- 九套回归（scene / world / ai / stuck / vehicle / loot / chase / flow / ship）：**全绿**。

## 实机验收步骤

1. 按 **F2** 或调试面板「立即降落飞船」召唤飞船并落地。
2. 走到任一**蓝色光柱**里站 **1.5 秒** → 飘起登舰。
3. 走到船中心黄色 **"操控室"** 驻留 **1.2 秒** → 触发劫持（全图广播）。
4. 飞船自动飞向最近**破解点** → 站在操控室 **4 分钟** → 整船撤离 + 密闭舱奖励（无视负重）。

## 待你拍板

- 你消息在"按照"处被截断了——我按"登舰后走不完 **劫持→破解→撤离**"理解并已修复，请确认是否就是这个意思？
- **O8**：飞船内是否要刷守卫制造风险？（现只撒富容器，无怪，偏安全）
- 自然降落 **290s** + 破解 **240s** 的压力曲线是否合适？
- 多队"夺取重激活"目前是**单机简化版**（劫持者离船即中断、破解点可再激活）；真正联机下的队伍夺取为后续 TODO。

## 改动文件

- `scripts/world/spaceship.gd`：`_pick_land_spot` / `_spot_pillars_clear` / 劫持判定放开 LANDED / 移除落地踢人 / `_draw` 重写
- `scripts/world/world_map.gd`：`spawn_ship_wreck_loot`（船内撒富容器）
- `tools/ship_test.gd`：A4 段对齐游戏真实落点路径
