# 飞船"看不见"答疑 + F2 调试召唤（v0.4 补丁）

> 对应工程：raid-proto-2d v0.4 单机验证版

## 结论：飞船没丢，是时效 + 小地图范围问题

| 你以为的 | 实际 |
|---|---|
| 飞船没生成 | 飞船**有生成**，只是开局 **5 分钟**（`spaceship_spawn_time=300`）才刷出——这是你定的"到达5分钟时地图出现飞船" |
| 小地图看不见 | 小地图是**局部**的（`minimap_range_m=700`，只显示你身边 700m）。飞船从地图左缘飞入，初始离你极远，右上角小地图自然不显示 |

**怎么确认它真的在**：按 **M** 开全屏地图，全图任意位置的飞船都会以蓝色三角（巡航中）或"富物资"光环（已降落）标出。

## 本次改动

1. **F2 = 立即召唤飞船**（调试键）
   - `world_map.gd` 新增 `force_spawn_spaceship()`：绕过 5 分钟计时，把飞船直接挪到你身边（右 520px）并**立刻降落**，落点用 `find_clear_circle` 选近旁空地、兜底原地降落。
   - 再按一次（若飞船还在巡航）会强制它立刻降落。
   - `world_map.gd` 新增 `_unhandled_input` 监听 `KEY_F2`。
   - `spaceship.gd` 新增 `force_descend(w)`。
   - HUD 提示行已加 `[F2] 召飞船`；`跑测自查清单.md` §六 加 **6.4b**。
2. **F1 调试面板里 `spaceship_spawn_time` 本就是滑块（60–600）**，也能临时调小。

## 验证（全绿）

- 编译：`--headless --path . --quit` 通过。
- 新增 `tools/ship_test.gd`（`--ship-test`）：直接调 `force_spawn_spaceship()`，断言 召唤前无飞船 → 召唤后立刻存在 → 跳过巡航进入降落 → 降落后生成动态 POI（25→26）。**全绿**。
- 回归 `flow_test`（`--flow-test`）复跑**全绿**。

## 下一步

回到你睡前下达的**扩展版飞船需求**（200m×100m 巨型载具 / 光柱吸入 / 地面舱+密闭舱 / 劫持操控室 / 破解点 4 分钟驻守 / 全图广播+弱信息 / 队伍夺取重激活）——目前仅读代码规划，尚未实现。
