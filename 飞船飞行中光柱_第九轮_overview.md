# 飞船「飞行中光柱 / 不等降落就能上传送劫持」修复 · 第九轮概览

> 面向策划（大锤）的变更说明，不涉代码细节。

## 你这次的诉求（纠正了上一轮的理解偏差）
飞船**飞行过程中就要一直有传送光柱**，你随时能站光柱传送到飞船内部、进操控室劫持，
**不是等飞船降落了再传送进去**。

## 之前为什么看不到光柱（根因）
- 飞船原逻辑是从地图左缘一路飞到地图 30%~72% 处巡弋，离你几千像素；光柱虽然画了，
  但跟着飞船飘在远处，你的视野（约 460px 扇形）里根本看不到、也走不进去。
- 按 `F2` 原语义是"召唤并**立即降落**"，直接跳过了飞行阶段——所以你更看不到"飞行中光柱"。

## 本轮修复
1. **飞船开局就飞到你头顶上方盘旋**：光柱始终垂在你视野里，你走两步站进蓝色光柱就能传上飞船、
   走到中心黄色「操控室」驻留即可劫持，飞船被劫持后飞向破解点——**全程在飞行中完成，不用等降落**。
   （飞船半宽远大于盘旋半径，你始终被船体笼罩，最近的光柱总在身边。）
2. **`F2` 语义改为"召唤飞船到头顶盘旋"**（飞行中光柱可见，可直接上传送劫持）。
   "立即降落"挪到**调试面板（F1）「立即降落飞船」按钮**，方便快速验收落地搜刮。
3. 新增两个可调参数（调试面板滑块）：盘旋半径 `spaceship_orbit_radius`（默认 450）、
   盘旋速度 `spaceship_orbit_speed`（默认 0.6）。

## 验证
- 飞船全链路测试 **17/17 全绿**，连跑 3 遍无 flake；其中新增 **E 段**专门覆盖你这次的核心诉求：
  「飞行中(CRUISE)站光柱登舰 + 操控室劫持」链路。
- 九套回归（scene / world / ai / stuck / vehicle / loot / chase / flow / ship）**全部通过**。

## 实机验收（你直接试）
- 等飞船刷出（默认 5 分钟，可调小 `spaceship_spawn_time`）或按 **F2** → 飞船飞到你头顶盘旋，
  看蓝色**光柱** → 走两步站进去 **1.5 秒** 登舰 → 走到中心黄色 **「操控室」** 驻留 **1.2 秒** 劫持 →
  飞船自动飞向破解点，你驻守 **4 分钟** → 整船撤离 + 密闭舱奖励（无视负重）。

## 待你拍板
1. 飞船默认 **5 分钟**才刷出（`spaceship_spawn_time=300`），是否太晚？想早点看到飞行中光柱可调小，
   或默认就用 F2 召唤。
2. 盘旋半径 450 / 速度 0.6 的体感是否合适（飞船绕你转的快慢、远近）？
3. 自然降落 290 秒 + 破解 240 秒压力曲线是否合适？（待多人对测）
4. 飞船内守卫是否要补（现只撒富容器、无怪，偏安全）？

## 改动文件
- `scripts/world/spaceship.gd`：飞船头顶刷出 + 绕玩家盘旋 + 落点以玩家为中心
- `scripts/world/world_map.gd`：F2 改为"召唤盘旋"
- `scripts/autoload/tuning.gd`：新增盘旋半径/速度参数
- `tools/ship_test.gd`：新增 E 段（飞行中登舰劫持）+ A4 瞬移落地 + A6 放宽
- `demo-2d/2D-Demo设计规范.md`：§七 / 参数表 / 变更日志同步
